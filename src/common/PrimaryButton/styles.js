import {StyleSheet} from 'react-native';
import {WHITE} from '../../values/Colors';

export default StyleSheet.create({
  main__container: {
    alignItems: 'center',
    paddingVertical: 12,
    borderRadius: 8,
  },
  btnText: {
    color: WHITE,
    fontSize: 16,
    fontWeight: '500',
  },
});
